[project]
name = "state-machine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "graphviz>=0.21",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
pythonVersion = "3.12"
